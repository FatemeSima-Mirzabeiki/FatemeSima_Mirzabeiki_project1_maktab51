def hashing_password(password):
    """
    :param password: the real password
    :return: hashed password
    this function get password, hash it and then return the hashed password
    """
    return password
